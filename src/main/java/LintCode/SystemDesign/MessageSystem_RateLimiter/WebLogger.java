package LintCode.SystemDesign.MessageSystem_RateLimiter;

/**
 * Implement a web logger, which provide two methods:
 * 
 * hit(timestamp), record a hit at given timestamp. get_hit_count_in_last_5_minutes(timestamp), get hit count in last 5
 * minutes. the two methods will be called with non-descending timestamp (in sec).
 */
public class WebLogger {
    public WebLogger() {
        // do intialization if necessary
    }

    /*
     * @param timestamp: An integer
     * @return: nothing
     */
    public void hit(int timestamp) {
        // write your code here
    }

    /*
     * @param timestamp: An integer
     * @return: An integer
     */
    public int get_hit_count_in_last_5_minutes(int timestamp) {
        // write your code here
        return 0;
    }
}